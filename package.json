{
  "name": "regula-rustica",
  "private": true,
  "scripts": {
    "build:cloud-config": "node scripts/generate-cloud-config.mjs",
    "check": "node --check app.js && node --check cloud-auth.js && node --check scripts/generate-cloud-config.mjs && node scripts/check-cloud-foundation.mjs"
  }
}
