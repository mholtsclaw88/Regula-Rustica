{
  "name": "regula-rustica",
  "private": true,
  "scripts": {
    "build:cloud-config": "node scripts/generate-cloud-config.mjs",
    "test:sync": "node --test tests/sync.test.mjs",
    "check": "node --check app.js && node --check cloud-auth.js && node --check service-worker.js && node --check sync/local-state.mjs && node --check sync/entities.mjs && node --check sync/cloud-adapter.mjs && node --check sync/engine.mjs && node --check sync/runtime.mjs && node --check scripts/generate-cloud-config.mjs && node scripts/check-cloud-foundation.mjs"
  }
}
